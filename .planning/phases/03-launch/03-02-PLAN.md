---
phase: 03-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/og-image.png
autonomous: false
requirements:
  - LAUNCH-02
must_haves:
  truths:
    - "public/og-image.png exists at 1200x630 pixels"
    - "OG image file is under 150 KB"
    - "OG image contains the studio name 'Apex Code'"
  artifacts:
    - path: "public/og-image.png"
      provides: "Social share preview image for OpenGraph"
      min_size_bytes: 1000
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "public/og-image.png"
      via: "openGraph.basic.image URL"
      pattern: "SITE_URL.*og-image.png"
---

<objective>
Create the OG image asset (public/og-image.png) that is declared in Layout.astro's SEO component but does not yet exist. Without this file, social share previews show no image and the browser logs a 404 on every page load.

Purpose: Required for LAUNCH-02 (opengraph.xyz shows correct preview) and contributes to LAUNCH-01 (eliminates 404 that could affect Lighthouse Best Practices).

Output: A 1200x630 PNG file at public/og-image.png.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-launch/03-RESEARCH.md
@src/layouts/Layout.astro
@src/lib/metadata.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create OG image asset</name>
  <read_first>
    - src/layouts/Layout.astro (see line 29 — openGraph.basic.image references /og-image.png)
    - src/lib/metadata.ts (SITE_TITLE and SITE_DESCRIPTION for text content)
  </read_first>
  <action>
    Create a PNG image file and save it to `public/og-image.png`.

    **Specification:**
    - Dimensions: exactly 1200 x 630 pixels (1.91:1 aspect ratio)
    - Format: PNG
    - File size: under 150 KB (target under 100 KB)
    - Design: white background (#ffffff), dark text (#0a0a0a), studio name "Apex Code" prominently displayed
    - Include tagline or value proposition (e.g., "AI-Powered Development for SMBs")
    - Keep text in the top 80% of the image (Facebook overlays domain name in the bottom strip)
    - No photos or illustrations needed — typography-forward matches the site design

    **Tools (pick one):**
    - Figma (free) — create 1200x630 frame, export as PNG
    - Canva (free) — use "Social Media" or custom 1200x630 template
    - Any image editor capable of PNG export at the correct dimensions

    **After creating the file:**
    Place the file at: `public/og-image.png` in the project root.

    This is a human-only task because it requires visual design judgment. Claude cannot create PNG image assets.
  </action>
  <acceptance_criteria>
    - `ls -la public/og-image.png` shows the file exists
    - File size is under 153600 bytes (150 KB)
    - Image dimensions are 1200x630 (verify in any image viewer or `file public/og-image.png`)
    - `npm run build && ls dist/og-image.png` confirms the image is copied to build output
  </acceptance_criteria>
  <verify>
    <automated>ls -la public/og-image.png && npm run build && ls dist/og-image.png && echo "OG IMAGE OK"</automated>
  </verify>
  <done>
    public/og-image.png exists at 1200x630 pixels, under 150 KB, and is copied to dist/ during build. The 404 for og-image.png is eliminated.
  </done>
</task>

</tasks>

<verification>
- `public/og-image.png` exists and is a valid PNG
- `npm run build` copies it to `dist/og-image.png`
- File is under 150 KB
</verification>

<success_criteria>
The OG image asset exists and will be served at the production URL, eliminating the 404 and enabling social share previews.
</success_criteria>

<output>
After completion, create `.planning/phases/03-launch/03-02-SUMMARY.md`
</output>
